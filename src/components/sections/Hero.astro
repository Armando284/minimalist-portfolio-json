---
import { basics } from '@cv'
import Section from '@/components/Section.astro'

const { name, label } = basics
---

<Section>
	<div class="container">
		<div class="bar"></div>
		<div class="info">
			<h1>{name}</h1>
			<h2>{label}</h2>
		</div>
	</div>
</Section>

<style>
	.container {
		display: flex;
		flex-direction: row;
		align-items: center;
		justify-content: start;
		gap: 1rem;
	}

	.bar {
		width: 44px;
		height: var(--hero-card-height);
		background: var(--color-custom-red-600);
		border-radius: 2px;
	}

	.info {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		padding-right: 32px;
	}

	h1 {
		font-size: 2rem;
		color: var(--color-custom-blue-700);
		font-weight: bold;
	}

	h2 {
		color: var(--color-custom-blue-700);
		font-weight: 500;
		font-size: 1.1rem;
		text-wrap: balance;
	}

	img {
		aspect-ratio: 1 / 1;
		object-fit: cover;
		width: 128px;
		border-radius: 16px;
	}

	span {
		color: #666;
		display: flex;
		align-items: center;
		gap: 0.25rem;
		font-size: 0.85rem;
		letter-spacing: -0.05rem;
	}

	@media print {
		.bar {
			-webkit-print-color-adjust: exact; /* Chrome/Safari */
			print-color-adjust: exact; /* Estándar */
			color-adjust: exact; /* Firefox */
		}
	}
</style>
